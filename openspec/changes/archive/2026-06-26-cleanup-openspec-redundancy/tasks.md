## 1. Remove redundant template

- [x] 1.1 Delete `openspec/changes/_template/` directory and all its contents

## 2. Rewrite README

- [x] 2.1 Replace `openspec/README.md` with a lean version (≤40 lines) covering: directory structure, what baseline is, and how to use the OpenSpec CLI commands
